"""
Iterator [Interador]

-> Um objeto que pode ser iterado.
-> Um objeto que retorna um dado, sendo um elemento por vez quando a funcao next() é chamada.


Iterables [Iteraveis]
> Um objeto que ira retornar um iterador quando a funcao iter() for chamada.

    listas = [1, 2, 3, 4]
    string = "Something"

"""